# docs-sync agent — 2026:05:19T17:46:05

- **Branch**: agent/docs-sync/2026-05-19
- **Started**: 2026-05-19T17:46:05Z
- **Finished**: 2026-05-19T17:55:00Z
- **Status**: completed

## Summary
- Compared all 20 workflow YAML files against their 19 deep-dive docs and the two macro docs (WORKFLOW_ARCHITECTURE.md, WORKFLOWS_INDEX.md).
- Found significant discrepancies in 10+ deep dives. Most critical: `agent-review-pr` (1 job vs 3 in source), `update-prs-with-development` (cron removed, dispatch-only), `docs-sync` (push trigger moved to separate trigger workflow), `publish-release` (missing run-examples-tests job), `tests` (development branch not a trigger).
- Fixed quick-reference-card factual errors in 3 deep dives (VITALS, TESTS, UPDATE_PRS) within the time budget.
- Ran out of time before addressing the deeper structural rewrites needed in agent-review-pr, publish-release, docs-sync, and the macro docs.

## Files Changed
- `.github/docs/VITALS_DEEP_DIVE.md` - removed incorrect `id-token: write` from quick reference permissions (synced against `vitals.yml`)
- `.github/docs/TESTS_DEEP_DIVE.md` - fixed trigger from `main, development` to `main` only in quick reference (synced against `tests.yml`)
- `.github/docs/UPDATE_PRS_DEEP_DIVE.md` - fixed trigger from cron to dispatch-only, removed phantom `id-token: write` permission, updated schedule line in quick reference (synced against `update-prs-with-development.yml`)

## Next Steps
### Critical (factually wrong - should fix next run)
1. **AGENT_REVIEW_PR_DEEP_DIVE.md** - Major rewrite needed. Source has 3 jobs (tests, review, decide) but doc describes 1 job. Job filter is `base_ref == 'development'` not `startsWith(head_ref, 'agent/')`. Missing: split-token pattern in decide job, `$PR_CONTEXT` substitution, job outputs, Upload agent prompt step.
2. **PUBLISH_RELEASE_DEEP_DIVE.md** - Missing entire `run-examples-tests` job (Node 22.x matrix, Examples Test environment, AWS secrets). Doc says 2 jobs, source has 3. Doc claims "no long-lived AWS keys" but `run-examples-tests` uses `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`.
3. **DOCS_SYNC_DEEP_DIVE.md** - Push trigger no longer in this file (moved to `docs-sync-trigger.yml`). Missing `.github/vitals/` watched path. Missing `allowed_bots` parameter. Clock-out condition is `always() && steps.prompt.outputs.log_file != ''`, not just `always()`. Missing Upload agent prompt step.
4. **WORKFLOW_ARCHITECTURE.md** - Says 16 workflows, actual 20. Missing catalog entries for docs-sync.yml, docs-sync-trigger.yml, vitals.yml. Wrong trigger matrix for tests.yml and update-prs. Missing docs-sync agent from tool/model table and agent roster. Missing AWS secrets from inventory. Missing docs-sync.md from filesystem map.
5. **WORKFLOWS_INDEX.md** - Missing docs-sync-trigger.yml row. Wrong trigger for update-prs-with-development. Wrong workflow count in header.
6. **TEST_PACKAGE_DEEP_DIVE.md** - Wrong `if` condition (doc shows compound expression, source is simple sender check). Missing 3 AWS secrets.
7. **docs-sync-trigger.yml** - New workflow file needs a new deep dive created.

### Medium (incomplete or slightly off)
8. **TESTS_DEEP_DIVE.md** - Prose/diagrams still reference development branch trigger (only quick ref card was fixed). Node 24 step count says 7, should be 6.
9. **UPDATE_PRS_DEEP_DIVE.md** - Prose/diagrams throughout still describe weekday cron (only quick ref card was fixed). Section 2 title still says "weekday cron and dispatch".
10. **PERSONAS_RUN_DEEP_DIVE.md** - Missing Upload agent prompt step in both run-persona and run-curator diagrams. Stale line number references (72-155 should be 72-163, 157-229 should be 165-245).
11. **AGENT_RUN_DEEP_DIVE.md** - Missing Upload agent prompt step from DAG diagram.
12. **DRAFT_MAIN_PR_DEEP_DIVE.md** - Doc says ~12 steps, actual is 15 named steps.
13. **PACK_PACKAGE_DEEP_DIVE.md** - Section 1 diagram says `actions/setup-node@v4` but actually uses composite `./.github/actions/setup-node`.
