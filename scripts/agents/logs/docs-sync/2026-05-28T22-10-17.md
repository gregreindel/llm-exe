# docs-sync agent - 2026:05:28T22:10:17

- **Branch**: agent/docs-sync/2026-05-28
- **Started**: 2026-05-28T22:10:17Z
- **Finished**: 2026-05-28T22:15:00Z
- **Status**: completed

## Summary

- Synced three release-pipeline deep dives to reflect new pre-release (`-beta.X`, `-rc.X`, `-alpha.X`) support introduced across `create-draft-release.yml`, `draft-main-pr.yml`, and `publish-release.yml`.
- `create-draft-release.yml` now validates an optional `-PRERELEASE` suffix, marks the GitHub release as `prerelease` and skips `make_latest` accordingly. Documented updated regex, POST body fields, and the new tag-format invariant.
- `draft-main-pr.yml` now short-circuits the auto-bump path when `package.json` declares a pre-release version; documented the new IS_PRERELEASE branch in the version-comparison flowchart, state machine, and failure modes.
- `publish-release.yml` no longer relies on a `beta` substring check. Documented the new multi-channel routing: first alphabetic chunk after `-` becomes the npm dist-tag; pre-releases with no alphabetic channel are refused outright.
- Refreshed cross-cutting macro docs (`WORKFLOW_ARCHITECTURE.md` section 9.16 and stage 5 table, plus `WORKFLOWS_INDEX.md` release-pipeline rows) so the catalog summaries match the new routing.

## Files Changed

- `.github/docs/CREATE_DRAFT_RELEASE_DEEP_DIVE.md` (synced against `.github/workflows/create-draft-release.yml`)
- `.github/docs/DRAFT_MAIN_PR_DEEP_DIVE.md` (synced against `.github/workflows/draft-main-pr.yml`)
- `.github/docs/PUBLISH_RELEASE_DEEP_DIVE.md` (synced against `.github/workflows/publish-release.yml`)
- `.github/docs/WORKFLOW_ARCHITECTURE.md` (catalog + stage table entries for publish-release)
- `.github/docs/WORKFLOWS_INDEX.md` (release pipeline rows: draft-main-pr, create-draft-release, publish-release)

## Next Steps

- The deprecated `npm run publish-beta` script is no longer referenced by the workflow (the new path calls `npm publish --provenance --tag $TAG` directly). If that script is also gone from `package.json`, the deep dive's "Publish (beta)" historical context can be trimmed in a future pass; left untouched here because docs-sync only touches docs.
- Consider whether `CHECK_SEMVER_DEEP_DIVE.md` needs a note about pre-release versions: `check-semantic-versioning.yml` was not in the change set, but its semver comparison logic likely needs to accept pre-release suffixes for the new flow to merge cleanly. Worth auditing on the next docs-sync run if `check-semantic-versioning.yml` is touched.
- `WORKFLOW_ARCHITECTURE.md` mentions `make_latest` and `prerelease` flag handling only in the publish-release section now; if the create-draft-release section ever gets a dedicated 9.x catalog entry detail line, fold in the new flag behavior.
