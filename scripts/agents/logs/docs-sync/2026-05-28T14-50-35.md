# docs-sync agent — 2026:05:28T14:50:35

- **Branch**: agent/docs-sync/2026-05-28
- **Started**: 2026-05-28T14:50:35Z
- **Finished**: 2026-05-28T14:54:00Z
- **Status**: completed

## Summary

- `.github/workflows/tests.yml` added a third trigger: `push: branches: ["main"]`. The YAML comments explain it is there so the Node 24.x Coveralls upload produces a coverage record tagged for `main`, which the docs-site badge filters with `?branch=main`.
- Updated `TESTS_DEEP_DIVE.md` to reflect the new trigger: section 1 whole-picture diagram, section 2 triggers diagram + prose + line refs, section 4 line refs, section 7 output cascade (badge wording), and the quick reference card.
- Updated `WORKFLOW_ARCHITECTURE.md` section 3 trigger matrix row and section 9.7 (`tests.yml` catalog entry) with the rationale for the push trigger and a clearer bypass note.
- Updated `WORKFLOWS_INDEX.md` CI hygiene row for `tests.yml`.
- Verified no em or en dashes in any updated file.

## Files Changed

- `.github/docs/TESTS_DEEP_DIVE.md` (synced against `.github/workflows/tests.yml`)
- `.github/docs/WORKFLOW_ARCHITECTURE.md` (synced against `.github/workflows/tests.yml`)
- `.github/docs/WORKFLOWS_INDEX.md` (synced against `.github/workflows/tests.yml`)

## Next Steps

- The bypass `if:` in `tests.yml` checks `pull_request.base.ref == 'development' && pull_request.head.ref == 'bump-version-branch'`. Because the workflow only triggers on `pull_request: branches: ["main"]`, that condition is structurally unreachable on PR events (a PR targeting `main` cannot have base `development`). Worth a separate investigation issue if it should be tightened or removed; out of scope for docs-sync.
- The Coveralls badge wording in `TESTS_DEEP_DIVE.md` section 7 references the docs-site badge specifically. If the README later regains a Coveralls badge, that section should be revisited.
- No other deep dives reference the tests.yml triggers, so nothing else was stale on this run.
