# docs-sync agent — 2026:05:19T20:58:34

- **Branch**: agent/docs-sync/2026-05-19
- **Started**: 2026-05-19T20:58:34Z
- **Finished**: —
- **Status**: running

## Summary
- Synced `AGENT_REVIEW_PR_DEEP_DIVE.md` after `agent-review-pr.yml` gained a `workflow_dispatch` trigger (with `pr_number`, `base_ref`, `head_ref` inputs) and a new `gh pr checkout` step in the tests job for dispatch events.
- Synced `BOT_RESPOND_DEEP_DIVE.md` after `bot-respond.yml` expanded from two modes to three: new Mode 1 dispatches `agent-review-pr.yml` for review requests instead of doing inline reviews.
- Updated `WORKFLOW_ARCHITECTURE.md` trigger matrix (agent-review-pr now has `workflow_dispatch: yes`), catalog entries for sections 9.4 and 9.6, and the agent role table rows for reviewer and bot-respond.
- Updated `WORKFLOWS_INDEX.md` trigger and description columns for both workflows.

## Files Changed
- `.github/docs/AGENT_REVIEW_PR_DEEP_DIVE.md` (synced against `agent-review-pr.yml`): sections 1, 2, 3, 4, 6, 7, 8, 12 updated for workflow_dispatch trigger, updated job conditions, new tests checkout step, and shifted line references.
- `.github/docs/BOT_RESPOND_DEEP_DIVE.md` (synced against `bot-respond.yml`): sections 1, 4, 5, 6, 9, 10, 11, 13 updated for three-mode split (dispatch review, read-only, write).
- `.github/docs/WORKFLOW_ARCHITECTURE.md`: trigger matrix row for agent-review-pr, catalog entries 9.4 and 9.6, agent role table rows.
- `.github/docs/WORKFLOWS_INDEX.md`: trigger and description columns for agent-review-pr and bot-respond.

## Next Steps
- The Mermaid sequenceDiagram in AGENT_REVIEW_PR section 4 does not yet show the workflow_dispatch entry path (it only shows the pull_request opened path). A future run could add an alt block for dispatch.
- Section 10 (state machine) in AGENT_REVIEW_PR still references "head_ref not starting with agent/" as a filter, but the actual job-level filter is on base_ref == development, not on head_ref prefix. This predates these changes but is worth correcting.
- WORKFLOW_ARCHITECTURE.md section 3 topology diagram (Mermaid) does not show the new bot-respond to agent-review-pr dispatch edge. A future run could add it.
