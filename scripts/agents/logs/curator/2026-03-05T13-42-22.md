# curator agent — 2026:03:05T13:42:22

- **Branch**: agent/curator/2026-03-05
- **Started**: 2026-03-05T19:42:22Z
- **Finished**: 2026-03-05T19:46:00Z
- **Status**: complete

## Summary

Reviewed 5 substantive persona logs (beginner x2, harsh-critic, speed-runner, enterprise) plus 4 empty/incomplete runs. Identified ~31 unique findings across all personas. Promoted 6 to GitHub issues, skipped the rest.

## Findings Reviewed

### PROMOTED

| # | Finding | Personas | Issue |
|---|---------|----------|-------|
| 1 | CLAUDE.md lists wrong mock LLM provider keys (`openai.chat-mock` / `openai.mock` vs actual `openai.chat-mock.v1`) | beginner, harsh-critic, speed-runner, enterprise | #146 |
| 2 | Boolean parser only accepts literal "true", rejects "yes"/"1"/"y" | beginner, speed-runner, enterprise | #147 |
| 3 | JSON parser silently returns `{}` on invalid input | beginner, enterprise | #148 |
| 4 | Executor result type resolves to `any` — type inference broken | beginner | #149 |
| 5 | `listToJson` parser truncates values containing colons (URLs, timestamps) | beginner | #150 |
| 6 | Invalid parser type silently falls back to StringParser instead of throwing | speed-runner | #151 |

### SKIPPED

| Finding | Reason |
|---------|--------|
| README lists parsers incompletely (`...etc`) | Minor docs gap, docs site exists for full reference |
| README example uses undefined `mySchema` | Overview brevity, not a tutorial |
| `markdownCodeBlocks` vs `markdownCodeBlock` undocumented | Names are self-explanatory |
| `replaceStringTemplate` parser undocumented | Docs issue, low impact |
| `createState()` silently ignores extra args | Standard JS behavior, TS catches it |
| `createStateItem` confusing error without default | TS signature makes this clear |
| examples/claude.md shows wrong APIs (Zod, wrong createParser) | Example file, not main docs |
| Dialogue `set*` vs Prompt `add*` naming | Design choice, changing would break API |
| `setMessageTurn` puts system after assistant | Design choice |
| JSDoc copy-paste errors in createParser/ChatPrompt | Rejected by user |
| `parseUserTemplates` default contradicts comment | Rejected by user |
| `ChatPrompt.validate()` is a no-op TODO | Maintainer already knows (TODO in source) |
| Commented-out code in ChatPrompt.format() | Housekeeping, not impactful |
| Invalid provider error doesn't suggest alternatives | Nice-to-have, low priority |
| Missing template vars render as empty string | Standard Handlebars behavior |
| `stringExtract` returns empty string on no match | Reasonable sentinel value |
| `prompt.format()` no-args TypeError | TS catches this at compile time |
| `executor.execute()` accepts wrong types at runtime | TS-first library, runtime coercion is intentional |
| `listToJson` camelCases keys silently | Design choice, convention |
| `listToJson` overwrites duplicate keys | Expected for single-object output |
| Number parser returns -1 on failure | Not filed (stopped early) |
| Hook errors silently swallowed | Not filed (stopped early) |
| Timeout error message misleading | Not filed (stopped early) |
| listToArray inconsistent prefix stripping | Not filed (stopped early) |
| JSON schema `validateSchema` opt-in confusing | Design choice, could document better |

## Files Changed

GitHub issues created:
- #146 — bug: CLAUDE.md lists wrong mock LLM provider keys
- #147 — enhancement: Boolean parser should handle common LLM boolean responses
- #148 — bug: JSON parser silently returns {} on invalid input
- #149 — bug: Executor result type resolves to any — type inference broken
- #150 — bug: listToJson parser truncates values containing colons
- #151 — bug: Invalid parser type silently falls back to StringParser

## Next Steps

**Not filed (stopped early by user):** 4 findings were triaged as PROMOTE but not filed:
- Number parser returns -1 for non-numeric input (ambiguous failure sentinel)
- Hook errors are silently swallowed (has TODO in source at `src/executor/_base.ts:182`)
- Timeout error message reads like LLM agent output, not a timeout
- listToArray strips `- ` prefixes but not `1. ` or `* ` prefixes

**Patterns across personas:**
- **Silent failures are the #1 theme.** JSON returns `{}`, boolean returns `false`, number returns `-1`, invalid parser type returns StringParser — all silently. Parsers need a consistent error/signal strategy for unparseable input.
- **CLAUDE.md accuracy.** The mock provider key issue hit all 4 personas. CLAUDE.md should be treated as code — inaccuracies here waste everyone's time.
- **Documentation gaps exist but are secondary.** Most undocumented features are discoverable from source. The real issues are behavioral surprises (silent failures, unexpected defaults).
