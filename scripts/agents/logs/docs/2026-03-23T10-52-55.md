# docs agent — 2026:03:23T10:52:55

- **Branch**: agent/docs/2026-03-23
- **Started**: 2026-03-23T10:52:55Z
- **Finished**: 2026-03-23T10:58:00Z
- **Status**: complete

## Summary
- Added "Custom Providers" and "Callable Executors" to the sidebar navigation — both pages existed but were unreachable from the sidebar.
- Documented `createCoreExecutor` in the executor docs — it was a public export with no documentation.
- Added deprecation warning for `LlmExecutorOpenAiFunctions` in the tool calling docs, directing users to `LlmExecutorWithFunctions`/`createLlmFunctionExecutor`.
- Audited all public exports against docs — confirmed 12 of 14 exports were already documented; addressed the 2 gaps above.

## Files Changed
- `docs/.vitepress/config.mts` — added sidebar entries for Custom Providers and Callable Executors
- `docs/executor/index.md` — added Core Executor section with `createCoreExecutor` usage
- `docs/executor/openai-functions.md` — added deprecation notice for `LlmExecutorOpenAiFunctions`

## Next Steps
- Review `docs/llm/custom.md` for completeness — it covers `useLlmConfiguration` and `createOpenAiCompatibleConfiguration` but could use more examples.
- Consider adding a "Callable Executors" link in the top nav under References.
- The `replaceStringTemplate` parser entry in `docs/parser/included-parsers.md` is sparse — could use a usage example.
- The examples/ directory has several files (e.g. `ListInList.ts`, `writeCodeFromSpec.ts`) that aren't referenced from any docs page.
- The `createCoreExecutor` options parameter (second arg) isn't documented yet — it supports the same `CoreExecutorExecuteOptions` as other executors.
