# docs agent — 2026:04:15T10:59:34

- **Branch**: agent/docs/2026-04-15
- **Started**: 2026-04-15T10:59:34Z
- **Finished**: 2026-04-15T11:02:00Z
- **Status**: complete

## Summary
- Addressed #384: expanded the `replaceStringTemplate` parser docs with a real use case (LLM picks a template, executor fills in original input via `attributes`) and a working reply-composer example.
- Addressed #407: rewrote the `listToJson` section to lead with "single flat object", added a `::: warning` block with the exact repro showing duplicate keys silently overwrite, and pointed users to `listToKeyValue` / `json` for multi-record cases.
- Documented the `keyTransform` and `schema` options on `listToJson` (previously undocumented).
- Updated the README parser one-liners to reflect the clarified behavior for `listToJson`, `listToKeyValue`, and `replaceStringTemplate`.

## Files Changed
- docs/parser/included-parsers.md — rewrote Replace String Template and List to JSON sections.
- readme.md — clarified the one-line descriptions for `listToJson`, `listToKeyValue`, and `replaceStringTemplate` in the Built-In Parsers block.

## Next Steps
- #385 (State API docs) — prior runs already added a README State section and a `docs/state/index.md` warning about `createStateItem`'s type-locking behavior. The source-level asks (making `createState()`'s no-arg signature explicit, improving the "Expected undefined, received string" error) remain open for the coder agent.
- #407 suggests a longer-term rename to `keyValueToObject` for the docs surface. That would either be an API rename (breaking, needs maintainer decision) or a rename of just the documented display name while keeping the API. Not attempted here — out of scope for docs-only.
- `listToKeyValue` docs in `docs/parser/included-parsers.md` are terse (one-line description). Could benefit from the same treatment — options table, explicit note that it preserves order and duplicates (the complement to the `listToJson` warning).
- `markdownCodeBlock` / `markdownCodeBlocks` docs are reasonable but the shared example response uses an escaped backtick (``` ``' ```) that renders oddly — worth a closer look next run.
- Consider adding a small "Choosing a parser" decision table at the top of `included-parsers.md` that maps common response shapes to the right parser — would have prevented both #384 and #407.
