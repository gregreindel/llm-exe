# docs agent — 2026:04:16T11:04:17

- **Branch**: agent/docs/2026-04-16
- **Started**: 2026-04-16T11:04:17Z
- **Finished**: 2026-04-16T11:07Z
- **Status**: complete

## Summary
- Clarified `listToJson` in `docs/parser/included-parsers.md`: it returns a single flat object, not a list of objects. Documented the `keyTransform` option and added a warning pointing users to `listToKeyValue` / `json` when they need a list of objects. (gregreindel/llm-exe#407)
- Expanded the `replaceStringTemplate` entry in `docs/parser/included-parsers.md` with a description, direct example, and an executor pipeline example showing the original-input-fill-in use case. (gregreindel/llm-exe#384)
- Added a fuller State section to `readme.md` covering `createState`, `createStateItem`, `createDialogue`, attributes, and context items — and noted that `createStateItem`'s default value is required (it establishes the item's type). Mirrored the default-required note in `docs/state/index.md`. (gregreindel/llm-exe#385, docs portion only)
- Commented on each of issues #407, #384, #385 with the plan before editing.
- Verified `npm run typecheck` still passes.

## Files Changed
- docs/parser/included-parsers.md
- docs/state/index.md
- readme.md

## Next Steps
- Issue #385 also has source-code asks (reject extra arg to `createState()`, improve the `createStateItem` undefined-default error message in `src/state/item.ts`). Out of scope for docs agent — coder agent should pick this up.
- Consider adding a dedicated `docs/state/state-items.md` page that fully mirrors the `BaseStateItem` API (`getValue`, `setValue`, `resetValue`, plus serialization). Today the state-item docs are spread between the index page and inline snippets.
- The parser docs' intra-page anchor links (`#list-to-key-value`, etc.) are fragile because header slugs depend on the renderer — a pass to confirm VitePress slug rules and fix any stale links would be worthwhile.
- `createDialogue` is exported at the top level but its standalone form (outside of `state.createDialogue`) is barely covered in the README — `docs/state/dialogue.md` exists but the README could link to it more explicitly.
- `replaceStringTemplate` does not yet appear in `examples/` — adding a small end-to-end example would help discoverability.
