# docs agent — 2026:05:26T13:43:58

- **Branch**: agent/docs/2026-05-26
- **Started**: 2026-05-26T13:43:58Z
- **Finished**: 2026-05-26T13:48:00Z
- **Status**: complete

## Summary
- Audited all doc files against the public API surface (src/index.ts exports) — individual vitepress docs are well-maintained
- Found `docs/public/llms-full.txt` significantly stale with wrong defaults, missing options, and an unresolved include directive
- Fixed Generic Options table: corrected `numOfAttempts` default (0→2), `temperature`/`maxTokens` defaults (0/500→undefined), added missing `topP`, `stopSequences`, `effort` options
- Fixed 6 provider-specific options tables: OpenAI (added missing options, fixed model default), Gemini (removed falsely-mapped options, added effort), xAI (removed stale n/stream/stop/user options), Ollama (corrected to model-only mapping), Deepseek (added missing options), Bedrock Anthropic (replaced unresolved include directive with actual options table)
- Updated parser sections: enriched `replaceStringTemplate` description, added `listToJson` options table, fixed `defineSchema` import style

## Files Changed
- `docs/public/llms-full.txt` — 8 sections updated to match current API docs

## Next Steps
- The `llms-full.txt` still concatenates the vitepress sources with some Vue component tags (e.g. `<ImportModelNames>`, `<GenericOutput>`) that won't render in plain text — a future run could strip or resolve these
- The Bedrock docs (`docs/llm/bedrock/index.md`) are thinner than other providers — could benefit from model shorthand examples and a note about which Bedrock-supported models have shorthands
- The `executor/openai-functions.md` page is very short — could add more examples for multi-provider tool calling
- Consider adding an Ollama-specific "Configuration" section noting the `OLLAMA_ENDPOINT` env var to the `llms-full.txt` Ollama section (the vitepress page has it)
