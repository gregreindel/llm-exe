# docs agent — 2026:03:30T11:03:15

- **Branch**: agent/docs/2026-03-30
- **Started**: 2026-03-30T11:03:15Z
- **Finished**: 2026-03-30T11:08Z
- **Status**: complete

## Summary
- Audited all LLM provider documentation against source code config files and TypeScript interfaces
- Fixed generic options table: added missing `topP`, `stopSequences`, `effort` options; corrected incorrect default values for `temperature` and `maxTokens`; removed stale commented-out OpenAI table
- Fixed xAI options table: removed 5 options not actually mapped in config (`n`, `stream`, `stop`, `presencePenalty`, `user`); added 3 missing options (`effort`, `useJson`, `stopSequences`)
- Fixed Deepseek options table: removed unmapped options (`n`, `stream`); added 5 missing options (`frequencyPenalty`, `logitBias`, `useJson`, `stopSequences`, `effort`)
- Fixed Gemini options table: removed `temperature`/`maxTokens`/`topP` (not mapped in Google config); added `effort` option with note about reasoning model support
- Fixed Ollama options table: clarified only `model`/`prompt` are mapped; removed misleading entries

## Files Changed
- `docs/llm/generic.md` — Updated generic options table with missing options and corrected defaults
- `docs/llm/xai.md` — Rewrote xAI-specific options to match actual config
- `docs/llm/deepseek.md` — Rewrote Deepseek-specific options to match actual config
- `docs/llm/gemini.md` — Rewrote Gemini-specific options to match actual config
- `docs/llm/ollama.md` — Rewrote Ollama-specific options to match actual config

## Next Steps
- **Guards API**: The `guards` namespace is exported (`isOutputResult`, `isFunctionCall`, `isToolCall`, etc.) but has zero documentation. Consider adding a `docs/utils/guards.md` page.
- **Ollama/Gemini missing options**: The Ollama and Gemini configs don't map common options like `temperature`, `maxTokens`, `topP`. This may be a source code gap worth filing as a bug/feature request.
- **Embeddings docs**: The `input` parameter for `createEmbedding` is not documented on the embeddings pages. The embedding documentation is minimal overall.
- **BaseExecutor / BasePrompt**: Both are exported but have no dedicated documentation pages for users who want to extend them.
- **`createOpenAiCompatibleConfiguration`**: Well-documented in custom.md but could be more discoverable from the main LLM index page.
- **State module**: `BaseStateItem` and `DefaultStateItem` exports lack detailed API documentation.
