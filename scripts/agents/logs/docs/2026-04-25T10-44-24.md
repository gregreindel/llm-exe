# docs agent — 2026:04:25T10:44:24

- **Branch**: agent/docs/2026-04-25
- **Started**: 2026-04-25T10:44:24Z
- **Finished**: 2026-04-25T10:48:00Z
- **Status**: complete

## Summary
- Addressed 3 open documentation issues (#384, #385, #407)
- Expanded `replaceStringTemplate` parser docs with clear description, usage example, and input/output samples (fixes #384)
- Clarified `listToJson` parser: added warning about single-object behavior, documented `keyTransform` and `schema` options, noted duplicate key overwrite behavior (fixes #407)
- Expanded README State section with `createState`, `createStateItem`, `createDialogue` examples and link to full docs (fixes #385)
- Updated README parser descriptions for `listToJson` and `replaceStringTemplate` to be more accurate

## Files Changed
- `docs/parser/included-parsers.md` — expanded `replaceStringTemplate` and `listToJson` sections
- `readme.md` — updated parser descriptions, expanded State section

## Next Steps
- Issue #385 also mentions a `createState()` signature issue (silently ignores arguments) and a confusing `createStateItem` error when no default is given — these are source code bugs, not doc issues. Consider filing separate bug issues for those.
- The `docs/state/index.md` page is solid but could benefit from documenting `deleteAttribute` and `clearAttributes` with examples.
- Consider adding a "Parsers at a Glance" comparison table to help users pick the right parser for their use case.
- The `listToKeyValue` parser section in `included-parsers.md` could use an options table similar to what was added for `listToJson`.
