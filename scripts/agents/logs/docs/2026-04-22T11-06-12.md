# docs agent — 2026:04:22T11:06:12

- **Branch**: agent/docs/2026-04-22
- **Started**: 2026-04-22T11:06:12Z
- **Finished**: —
- **Status**: running

## Summary
- Addressed all 3 open documentation issues (#384, #385, #407)
- Expanded `replaceStringTemplate` parser docs with full description, executor pipeline example, and input/output block (#384)
- Clarified `listToJson` parser docs: explicitly states it returns a single flat object, added warning about duplicate key overwriting, documented `keyTransform`/`schema`/`validateSchema` options with examples (#407)
- Expanded State API docs in both `docs/state/index.md` and `readme.md`: covers `createState`, `createStateItem`, `createDialogue`, context, attributes, serialization, and all state methods with tables (#385)

## Files Changed
- `docs/parser/included-parsers.md` — expanded replaceStringTemplate and listToJson sections
- `docs/state/index.md` — rewrote state docs with full API coverage
- `readme.md` — expanded State section with createState/createStateItem/createDialogue examples

## Next Steps
- Issue #385 mentions a confusing error message in `createStateItem` when called without a default value — this is a source code concern, not docs. Consider filing a separate `bug` issue for improving the error message in `src/state/item.ts`.
- The `listToKeyValue` parser docs in `included-parsers.md` could use options documentation (similar to the treatment given to `listToJson` this run).
- The `json` parser section in `included-parsers.md` is fairly sparse — could benefit from documenting `schema` and `validateSchema` options with examples.
- The `stringExtract` parser options table is good, but the other simple parsers (string, number, boolean) could use `createParser` usage examples for consistency.
