# docs agent — 2026:04:18T10:41:02

- **Branch**: agent/docs/2026-04-18
- **Started**: 2026-04-18T10:41:02Z
- **Finished**: —
- **Status**: running

## Summary
- Addressed 3 open documentation issues: #384, #385, #407
- Expanded `replaceStringTemplate` parser docs with full description and usage examples (#384)
- Clarified `listToJson` parser docs: returns a single flat object (not an array), documented duplicate-key behavior, added `keyTransform`/`schema`/`validateSchema` options, added colon-in-value examples (#407)
- Verified state docs (docs/state/) are already comprehensive for #385; filed bug #422 for the `createStateItem` undefined default issue mentioned in #385
- Commented on all 3 issues with plans/status

## Files Changed
- `docs/parser/included-parsers.md` — expanded `replaceStringTemplate` section, improved `listToJson` section

## Next Steps
- Issue #385 asks for state docs in the README, but there is no README.md — the docs site at docs/state/ already covers the API. Consider whether a README.md should be added to the project root with a state section, or whether #385 can be closed as the docs site covers it.
- The `listToKeyValue` parser section could benefit from the same options documentation treatment given to `listToJson` (options table, edge case examples)
- Consider adding a "Choosing the right parser" guide or comparison table to help users pick between `listToJson`, `listToKeyValue`, `json`, etc.
- The `json` parser section is thin — could benefit from documenting schema enforcement and `defineSchema` usage with examples
