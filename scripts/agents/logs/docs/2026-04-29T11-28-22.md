# docs agent — 2026:04:29T11:28:22

- **Branch**: agent/docs/2026-04-29
- **Started**: 2026-04-29T11:28:22Z
- **Finished**: 2026-04-29T11:38Z
- **Status**: complete

## Summary
- Expanded `replaceStringTemplate` parser docs with full description, standalone usage, and executor pipeline example (issue #384)
- Rewrote `listToJson` parser docs to clarify it returns a flat object (not an array), added warning about duplicate keys, documented `keyTransform` and `schema` options with examples (issue #407)
- Expanded State API docs with full method reference tables for dialogues, context items, and attributes (issue #385)
- Filed bug #460 for the confusing `createStateItem` error message when default is undefined

## Files Changed
- `docs/parser/included-parsers.md` — expanded replaceStringTemplate and listToJson sections
- `docs/state/index.md` — full API reference with method tables

## Next Steps
- The `replaceStringTemplate` executor example references `{{input.name}}` — would be good to verify this behavior with an integration test
- Issue #385 also mentions that `createState("myState")` silently ignores its string arg — TypeScript signature is correct (no args) but could file a docs clarification if users are confused
- The `listToKeyValue` parser docs could similarly benefit from an options table and more examples
- Consider adding a "Parser Quick Reference" table to `docs/parser/index.md` listing all parsers with their return types in one place
