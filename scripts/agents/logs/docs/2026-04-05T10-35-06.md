# docs agent — 2026:04:05T10:35:06

- **Branch**: agent/docs/2026-04-05
- **Started**: 2026-04-05T10:35:06Z
- **Finished**: 2026-04-05T10:40:00Z
- **Status**: complete

## Summary
- Fixed incorrect default import `import llmExe from "llm-exe"` to `import * as llmExe from "llm-exe"` in install docs (no default export exists)
- Fixed incorrect function call `piiDetector({ input })` to `piiDetector(input)` in what_is_llm_function example (function takes string, not object)
- Fixed home page: incorrect Ollama model key `ollama.llama-3.3-70b` to `ollama.llama3.3`
- Fixed home page: moved retry/timeout config from executor options (where they don't exist) to LLM options, and corrected `maxRetries` to `numOfAttempts`
- Fixed home page: incorrect hook name `"complete"` to `"onComplete"` in `analyst.on()` call
- Applied same fixes to `docs/public/llms-full.txt` (compiled docs mirror)

## Files Changed
- `docs/intro/install.md` — fixed default import to namespace import
- `docs/intro/what_is_llm_function.md` — fixed function call example
- `docs/index.md` — fixed Ollama model key, executor options, and hook name
- `docs/public/llms-full.txt` — mirrored install and what_is_llm_function fixes

## Next Steps
- Review `docs/examples/` pages for accuracy against current API (not checked this run)
- The `docs/prompt/advanced.md` DialogueHistory example has all messages with role "user" — two should be "assistant" (lines 78, 82)
- Consider documenting `createCoreExecutor` more thoroughly — it's mentioned in executor/index.md but has no dedicated page
- Consider adding a doc page for `defineSchema` utility beyond the brief mention in parser/index.md
- The `docs/public/llms-full.txt` file is large (3156 lines) and appears to be manually maintained — may need a build script to generate from source docs
- Bedrock docs could list available model shorthand keys (currently only shows generic provider keys)
- The `effort` option for reasoning models could use more examples showing which models support it
