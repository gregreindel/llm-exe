# docs agent — 2026:03:12T10:40:08

- **Branch**: agent/docs/2026-03-12
- **Started**: 2026-03-12T10:40:08Z
- **Finished**: 2026-03-12T10:45:00Z
- **Status**: complete

## Summary
- Updated `docs/executor/openai-functions.md` to use the `createLlmFunctionExecutor` factory function instead of `new LlmExecutorWithFunctions()`, matching the public API export.
- Added `createCoreExecutor` documentation to `docs/executor/index.md` — this exported function was previously undocumented.
- Added global `registerHelpers`/`registerPartials` documentation to `docs/prompt/advanced.md` — previously only the per-prompt registration was shown.

## Files Changed
- `docs/executor/openai-functions.md` — updated to use `createLlmFunctionExecutor` factory, added import statement, updated title
- `docs/executor/index.md` — added Core Executor section with example
- `docs/prompt/advanced.md` — added section on global helper/partial registration

## Next Steps
- `createCoreExecutor` options parameter (`CoreExecutorExecuteOptions`) could use deeper documentation if it has user-facing options.
- The executor options page (`docs/executor/options.md`) should be reviewed — was not checked this run.
- `defineSchema` is only referenced in passing (parser docs, examples). Consider adding a dedicated utility reference section or expanding coverage in the parser docs.
- The `examples/` directory has `.test.ts` files for some examples but not all. Verify all example code compiles.
- `docs/misc/getting-started.md` and `docs/misc/llm-exe-aws-sfn.md` appear to be empty/stubs — should be filled or removed.
- VitePress sidebar could add a link to the new Core Executor section if it gets its own page in the future.
