# docs agent — 2026:04:27T11:44:03

- **Branch**: agent/docs/2026-04-27
- **Started**: 2026-04-27T11:44:03Z
- **Finished**: 2026-04-27T11:48:00Z
- **Status**: complete

## Summary
- Expanded `replaceStringTemplate` parser docs with full description, executor pipeline example, and input/output demo (closes #384)
- Clarified `listToJson` parser returns a single flat object, documented `keyTransform`/`schema`/`validateSchema` options, added duplicate-key warning (closes #407)
- Added warning to state docs about `createStateItem` requiring a typed default value to avoid confusing runtime errors (partially addresses #385)
- Filed bug #452 for the `createStateItem` undefined-default error message issue found in #385

## Files Changed
- `docs/parser/included-parsers.md` — expanded replaceStringTemplate and listToJson sections
- `docs/state/index.md` — added createStateItem default value warning

## Next Steps
- #385 asks for State API coverage in the README, but there is no README.md. The `docs/state/` pages already cover the API well. Consider whether a README.md should be added or if the issue can be closed with the existing docs site.
- The `add*` method aliases on Dialogue (addUserMessage, addAssistantMessage, etc.) are not documented in `docs/state/dialogue.md` — only the `set*` versions appear in the method table. A future run should add these.
- The `addFromOutput` method on Dialogue could use a more detailed example showing tool call handling.
- The `listToJson` parser's name-vs-behavior mismatch (#407) is documented now but a future major version could consider renaming to something like `keyValueToObject` for clarity.
- The `createStateFrom` function (in `src/state/_functions.ts`) is not exported from the public API but may be useful — worth considering for a future minor release.
