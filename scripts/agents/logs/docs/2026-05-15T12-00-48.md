# docs agent — 2026:05:15T12:00:48

- **Branch**: agent/docs/2026-05-15
- **Started**: 2026-05-15T12:00:48Z
- **Finished**: 2026-05-15T12:07:00Z
- **Status**: complete

## Summary
- Audited all public API exports against documentation — 9/10 are well-documented, only `LlmNativeFunctionParser` is missing (but it's deprecated in favor of `LlmFunctionParser`)
- Fixed `models.json` to match source configs: added `gpt-4` (OpenAI) and `grok-4-1-fast` (xAI) shorthands, removed `claude-3-haiku` which has no source config
- Added `createParser` usage examples for the `string` and `json` parsers in the included parsers doc, plus documented `json` parser options
- Added missing "Executor Options" page link to `llms.txt` index
- Verified all 7 LLM provider docs match source; no stale model references found in doc prose

## Files Changed
- `docs/.vitepress/data/models.json` — synced model shorthand lists with source configs
- `docs/parser/included-parsers.md` — added usage examples for string/json parsers, documented json parser options
- `docs/public/llms.txt` — added Executor Options link, capitalized Hooks link

## Next Steps
- `LlmNativeFunctionParser` is exported but undocumented. It's deprecated in favor of `LlmFunctionParser` — consider either documenting it briefly with a deprecation notice or removing the export in the next major version.
- The `llms-full.txt` file should be regenerated periodically to stay in sync with the VitePress docs (it appears to be a manual snapshot).
- The Anthropic options table could note that `temperature`, `topP`, and `topK` are automatically dropped for models that reject sampling params (e.g., `claude-opus-4-7`), and that Claude 4.x models drop `topP` when `temperature` is set. This behavior exists in source but isn't documented.
- Consider adding a `createParser("listToArray")` and `createParser("listToKeyValue")` usage example to those parser sections for consistency (they exist but lack the `createParser` call snippet).
