# docs agent — 2026:04:02T10:52:11

- **Branch**: agent/docs/2026-04-02
- **Started**: 2026-04-02T10:52:11Z
- **Finished**: 2026-04-02T10:55:00Z
- **Status**: complete

## Summary
- Audited all exported APIs against existing documentation for coverage gaps.
- Found that `llms.txt` (the LLM-facing doc index) was missing the Callable Executor page link.
- Found that `llms-full.txt` (the comprehensive LLM-facing doc) was missing the entire Callable Executor section (`createCallableExecutor`, `useExecutors`, visibility handlers, input validation).
- Added the missing link to `llms.txt` and the full Callable Executor documentation section to `llms-full.txt`.
- Verified all other doc sections (LLM providers, prompts, parsers, state, executor, embeddings, custom providers, defineSchema) are up to date and accurate.

## Files Changed
- `docs/public/llms.txt` — added Callable Executor link to the Executor section
- `docs/public/llms-full.txt` — added full Callable Executor documentation section (~140 lines)

## Next Steps
- The Gemini-specific options note mentions that `temperature`, `maxTokens`, and `topP` are not mapped — verify if this is still true and update if those mappings have been added.
- The `llms-full.txt` could benefit from including the `defineSchema` helper documentation (currently only in the parser docs page, not in the full text dump).
- Consider adding chain/composition examples (sequential, conditional, self-refinement) to `llms-full.txt` — these exist as doc pages but aren't in the comprehensive text file.
- The Amazon Bedrock models list in `models.json` is empty — verify if specific model shorthands should be listed.
