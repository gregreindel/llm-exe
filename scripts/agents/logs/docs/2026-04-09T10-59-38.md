# docs agent — 2026:04:09T10:59:38

- **Branch**: agent/docs/2026-04-09
- **Started**: 2026-04-09T10:59:38Z
- **Finished**: 2026-04-09T11:03:00Z
- **Status**: finished

## Summary
- Addressed the two open `documentation` issues (#384, #385) reported by the beginner persona.
- Expanded the README's State section to cover the full public surface: `createDialogue`, `createState`, `createStateItem`, context items, attributes, and serialize — replacing the old four-line dialogue stub.
- Beefed up `docs/parser/included-parsers.md` for `replaceStringTemplate`: added description, direct parser example, and an executor-level example showing the "LLM returns a template, parser fills in executor input" pattern.
- Added a warning to `docs/state/index.md` explaining that `createStateItem` locks its type to the default value — consumers should always pass a meaningful default of the target type.
- Sharpened the README parser-list one-liner for `replaceStringTemplate` so its purpose is visible at a glance.
- Verified: `npm run typecheck` passes.

## Files Changed
- `readme.md` — expanded State section, sharper `replaceStringTemplate` list entry.
- `docs/parser/included-parsers.md` — filled in `replaceStringTemplate` with description + examples.
- `docs/state/index.md` — added warning about `createStateItem` default-value type locking.

## Next Steps
- **Source fixes (coder agent):** Issue #385 also asks for two source changes that are out of scope for docs:
  - Either remove the unused argument from `createState(name?)` or make the signature explicit. Currently `createState("foo")` type-checks but silently drops the name.
  - Improve the `createStateItem` error message in `src/state/item.ts:16-17`. The `"Invalid value type. Expected undefined, received string"` error is baffling when the item was created without a default — message should nudge toward "pass a default value of the type you intend to store".
- **Docs:** The `docs/state/index.md` page covers context/attributes but doesn't yet document `useDialogue` vs `createDialogue` vs `getDialogue` semantics at the *state level* in a comparison table. Worth a small follow-up to reduce confusion (current text mentions them in prose but a table would make the differences pop).
- **Docs:** Nothing in the README or docs mentions `LlmExeError` / typed error surfaces. Low priority, but worth flagging for a later pass.
- **Examples:** The `examples/` folder does not yet have a `replaceStringTemplate` example file. A runnable example would complement the doc additions.
- No open `documentation` issues remain after this run.
