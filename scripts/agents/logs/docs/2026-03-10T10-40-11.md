# docs agent — 2026:03:10T10:40:11

- **Branch**: agent/docs/2026-03-10
- **Started**: 2026-03-10T10:40:11Z
- **Finished**: 2026-03-10T10:47:00Z
- **Status**: complete

## Summary
- Fixed executor hooks docs using `new LlmExecutor()` (not a public export) — replaced with `createLlmExecutor()`
- Fixed missing `createLlmExecutor` import in executor basic examples
- Corrected wrong parameter description for `llm` in executor docs (said "Use createPrompt" instead of "Use useLlm()")
- Fixed invalid JavaScript syntax in `registerPartials` example (`[name: ...]` → `[{ name: ... }]`)
- Applied all fixes to `docs/public/llms-full.txt` mirror

## Files Changed
- `docs/executor/hooks.md` — replaced `new LlmExecutor` with `createLlmExecutor` (3 instances)
- `docs/executor/index.md` — added missing import, fixed llm param description
- `docs/prompt/why-handlebars.md` — fixed `registerPartials` syntax
- `docs/public/llms-full.txt` — mirrored all above fixes

## Next Steps
- Add dedicated documentation for `defineSchema` utility — currently only shown in examples, no explanation of purpose (type-safe schema with `additionalProperties: false`)
- Add documentation for global `registerHelpers()` and `registerPartials()` utility functions — distinct from the inline `{ helpers, partials }` option when creating prompts
- Review `docs/executor/openai-functions.md` — uses `new LlmExecutorWithFunctions()` which IS exported, but could benefit from a `createLlmFunctionExecutor()` example if that's the preferred pattern
- Review provider-specific docs (anthropic, gemini, xai, deepseek, bedrock) for similar import/API accuracy issues
- Consider documenting `createCoreExecutor` — exported but appears undocumented
