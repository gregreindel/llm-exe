# docs agent — 2026:04:13T11:17:50

- **Branch**: agent/docs/2026-04-13
- **Started**: 2026-04-13T11:17:50Z
- **Finished**: 2026-04-13T11:21:00Z
- **Status**: completed

## Summary
- Addressed #384 (replaceStringTemplate undocumented): expanded the parser's section in `docs/parser/included-parsers.md` from a one-line stub to a full entry with a standalone example, an "when to use it" paragraph, and an executor example that clarifies how template data is accessed as `{{input.fieldName}}` via the parser's metadata argument.
- Addressed #385 (State API missing from README): replaced the dialogue-only snippet in the README's State section with an overview that covers `createState`, `createDialogue` (standalone and on state), `createStateItem` + context items, and `setAttribute`/`deleteAttribute`/`clearAttributes`. Added a pointer to the full `docs/state/` pages for depth.
- Left a planning comment on each issue before starting and noted that the non-doc asks in #385 (the `createState()` signature question and the `createStateItem` error message) are source changes belonging to the coder agent.
- Verified TypeScript examples via `npm run typecheck` (passes clean).

## Files Changed
- `readme.md`
- `docs/parser/included-parsers.md`
- `scripts/agents/logs/docs/2026-04-13T11-17-50.md`

## Next Steps
- #385 still has two source-code asks pending for the coder agent: (1) decide whether `createState()` should accept a name or reject the argument at the type level, and (2) improve the `createStateItem` error message when default is undefined. Once the source is decided, docs may need a follow-up pass.
- `docs/state/index.md` and `docs/state/dialogue.md` are solid, but there is no dedicated page covering `createStateItem`, context items, and attributes as a group. Consider splitting these into their own pages (e.g. `docs/state/context.md`, `docs/state/attributes.md`) linked from the state index, mirroring the dialogue page structure.
- The README still covers `createChatPrompt` only briefly. A short "Prompt" callout alongside the new State one — showing `addUserMessage`, partials/handlebars helpers — would make the README feel more balanced; today the parser section is by far the most detailed.
- The `docs/parser/included-parsers.md` page mixes `::: code-group` blocks that use backticks inconsistently (some use straight single-quote closers `` ``' ``, see `markdownCodeBlock` / `markdownCodeBlocks` examples). Worth a small cleanup pass to normalize.
- Worth auditing `docs/llm/` and `docs/executor/` next run — they were not touched this session.
