# docs agent — 2026:04:23T11:10:18

- **Branch**: agent/docs/2026-04-23
- **Started**: 2026-04-23T11:10:18Z
- **Finished**: 2026-04-23T11:15:00Z
- **Status**: complete

## Summary
- Addressed all 3 open documentation issues (#384, #385, #407).
- **#384**: Expanded the `replaceStringTemplate` parser section in `included-parsers.md` with a full description, code example, and input/output blocks.
- **#407**: Rewrote the `listToJson` parser section to clarify it returns a flat object (not an array), documented the `keyTransform` and `schema` options with an options table, and added an example for `keyTransform: "preserve"`.
- **#385**: Expanded the State section in `readme.md` to cover `createState`, `createStateItem`, and `createDialogue` with a comprehensive example showing dialogues, typed context items, and attributes.

## Files Changed
- `docs/parser/included-parsers.md` — expanded `replaceStringTemplate` and `listToJson` sections
- `readme.md` — expanded State section with full API examples

## Next Steps
- The existing docs site (`docs/`) has good coverage overall. Consider adding a dedicated page for `createCustomParser` with more advanced examples (extracting structured data, chaining parsers).
- The `embeddings/` docs may need review — `createEmbedding` is exported but docs only cover provider setup, not usage via the main API.
- Consider adding docs for `useLlmConfiguration` and `createOpenAiCompatibleConfiguration` — both are exported but not documented on the docs site.
- The `callable/` docs (`useExecutors`, `createCallableExecutor`) could use more examples showing real-world composition patterns.
