# docs agent — 2026:05:02T10:53:05

- **Branch**: agent/docs/2026-05-02
- **Started**: 2026-05-02T10:53:05Z
- **Finished**: 2026-05-02T10:57Z
- **Status**: complete

## Summary
- Addressed all 3 open `documentation` issues (#384, #385, #407)
- Added full documentation for the `replaceStringTemplate` parser with usage example, Handlebars explanation, and input/output code group (closes #384)
- Rewrote `listToJson` parser section to clarify it returns a flat object (not an array), documented `keyTransform`, `schema`, and `validateSchema` options with examples (closes #407)
- Expanded the State section in README with `createState`, `createStateItem`, and `createDialogue` examples covering dialogues, context items, and attributes (closes #385)

## Files Changed
- `docs/parser/included-parsers.md` — expanded `replaceStringTemplate` and `listToJson` sections
- `readme.md` — expanded State section with full API examples

## Next Steps
- The `markdownCodeBlock` section has a rendering issue — the closing triple backtick in the Response code group is escaped as `` ``' `` instead of actual backticks. This is likely a VitePress workaround but may render incorrectly.
- The `stringExtract` parser options table documents `ignoreCase` but it would be worth verifying this option still exists in the source (it was present in older docs but I didn't check source).
- The `json` parser section is minimal — it could use a proper code example showing `createParser("json", { schema })` with `defineSchema`, similar to the README's overview section.
- Consider adding a cross-link from the parsers doc page to the custom parser page, since `createCustomParser` is a major export.
- The `docs/state/index.md` page could benefit from documenting `state.getAttribute()` and `state.clearAttributes()` methods more explicitly with examples.
