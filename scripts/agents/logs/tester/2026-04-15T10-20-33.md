# tester agent — 2026:04:15T10:20:33

- **Branch**: agent/tester/2026-04-15
- **Started**: 2026-04-15T10:20:33Z
- **Finished**: 2026-04-15T10:28:00Z
- **Status**: complete

## Summary

- Starting coverage was already ~100% across the board (99.96% stmts, 99.9% branch, 100% funcs, 100% lines). No un-tested source files existed.
- Focused on **depth over breadth**: added regression tests that pin existing (buggy) behavior for two open bugs — gregreindel/llm-exe#393 (`createState()` silently ignores args) and gregreindel/llm-exe#394 (`createStateItem` without a default makes `setValue` fail with a confusing "Expected undefined" message). If/when those bugs are fixed, these tests will fail and force a docs/test update.
- Added edge-case coverage for `BaseStateItem.setValue` around the `typeof` guard's permissive treatment of `null`/`array`/`object`, and for `resetValue` reference semantics (initial value is not deep-cloned — pinning the current footgun).
- Added `createDialogue` and `createStateItem` unit tests that were previously implicit in `createStateFrom` / base-state tests.
- Full suite: **1608 tests pass** (up from 1595). `npm run typecheck` clean.

## Files Changed

- `src/state/_functions.test.ts` — added tests for `createState`, `createDialogue`, and `createStateItem` factory functions, including regression tests for issues #393 and #394.
- `src/state/item.test.ts` — added `setValue` type-guard edge cases (null, array-vs-object, cross-type rejections) and `resetValue` reference-semantics tests.

## Next Steps

- **Bug regression coverage is still thin for other open bugs.** Worth adding tests (pinning current behavior or documenting expected behavior) for:
  - gregreindel/llm-exe#382 / #370 / #366 — `effort` option only matches the exact model name `'gpt-5'`.
  - gregreindel/llm-exe#378 — type definitions include model shorthands not exported in config.
  - gregreindel/llm-exe#389 — assistant messages are always template-parsed (injection risk).
  - gregreindel/llm-exe#404 — prompt templates silently render empty for missing input variables.
- **Coverage is maxed by the metric but not by meaning.** Several modules have 100% line coverage but only happy-path assertions. Candidates for deeper edge-case testing:
  - `src/llm/config/openai/openai.ts` — model-specific option transforms (effort, reasoning, etc.) under mixed-case / unknown-model inputs.
  - `src/llm/output/**` — unusual/empty response shapes, partial tool-call payloads, multiple content blocks in odd orders.
  - `src/prompt/_base.ts` — Handlebars rendering with missing variables, async helpers under rejection, partials that throw.
  - `src/utils/modules/request.ts` / `requestWrapper.ts` — retry/timeout interactions (known warning in CLAUDE.md about a potential timer leak in `asyncCallWithTimeout.test.ts`).
- **Worker-exit warning** in the Jest suite (flagged in CLAUDE.md) was not investigated this run — next tester should isolate `asyncCallWithTimeout.test.ts` timer behavior.
- **Scenario tests**: `src/__scenarios__/` has two scenarios (`mock.scenarios`, `multi-tool.scenario`). Real provider shapes diverge; consider adding scenarios that exercise Anthropic tool-use results, Google Gemini function-call streams, and Bedrock-wrapped Anthropic output.
