# tester agent — 2026:05:28T12:30:16

- **Branch**: agent/tester/2026-05-28
- **Started**: 2026-05-28T12:30:16Z
- **Finished**: 2026-05-28T12:37:00Z
- **Status**: complete

## Summary
- Coverage going in was already at 99.96% statements / 99.91% branches / 100% lines / 100% functions, so this run focused on **behavioral coverage** that prevents regressions, not raw line coverage.
- Strengthened `JsonParser.test.ts`: added 16 tests documenting how the parser handles malformed JSON, top-level primitives, top-level arrays, markdown-wrapped JSON, schema enforcement without `validateSchema`, and strict-mode validation errors. These lock in current behavior referenced by open issue #148 ahead of any v3 break.
- Strengthened `StringExtractParser.test.ts`: added 12 tests covering enum match priority (enum order, not text order), case-insensitive ordering, null/undefined/number input rejection, empty input, and the (foot-gunny) raw-regex semantics of enum values — documents that `a.b` enum entry matches `axb` because the implementation does not escape regex metacharacters.
- Full suite: **1781 passing** (was 1753). Typecheck clean.

## Files Changed
- `src/parser/parsers/JsonParser.test.ts` — added malformed-input, markdown-wrapped, schema enforcement, and strict-validation suites.
- `src/parser/parsers/StringExtractParser.test.ts` — added match priority, input-shape, and partial-match (raw regex) suites.
- `scripts/agents/logs/tester/2026-05-28T12-30-16.md` — this log.

## Next Steps
- **Issue #410 has an active fix plan** (silent coercion of non-object input). When the coder agent lands the `TypeError` guard for `null`/`undefined`, update `src/executor/llm.test.ts` and `src/executor/_base.test.ts` to assert the new throw rather than the current silent coercion. Don't pre-emptively change these tests — they would conflict with the in-flight work.
- `StringExtractParser` not escaping regex metacharacters in enum values is a real footgun (now documented by a test). Worth filing a bug to evaluate whether to escape by default or expose a flag; this would be a `breaking` change so it belongs on the `v3.0.0` milestone.
- `helpJsonMarkup` has a known offset bug with leading whitespace before the ```json fence (already documented in `src/utils/modules/json.test.ts`). Worth a tracking issue if there isn't one.
- `MarkdownCodeBlocks` parser, `ReplaceStringTemplateParser`, and `LlmNativeFunctionParser` could use the same behavioral-edge-case treatment in a future session — they have line coverage but light edge-case coverage.
- Executor composition tests (`src/executor/executor-composition.test.ts`) already exist; a follow-up could verify type inference at the boundary (e.g., that parser-typed output flows through a second executor's input generic without `any` leaking) — Jest can do this with `expectTypeOf` patterns or `// @ts-expect-error` assertions.
