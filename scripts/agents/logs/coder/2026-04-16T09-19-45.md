# coder agent — 2026:04:16T09:19:45

- **Branch**: agent/coder/2026-04-16-issue-410
- **Started**: 2026-04-16T09:19:45Z
- **Finished**: 2026-04-16T09:27:00Z
- **Status**: done

## Summary
- Fixed #410: `execute()` silently coercing `null` input at runtime.
- Added a `TypeError` guard in `BaseExecutor.getHandlerInput` and `LlmExecutor.execute` for explicit `null` input, naming the executor in the error message.
- Kept the intentional coercion for `undefined` (no-arg convenience for prompts with no template variables) and for primitives/arrays (used by tool/function callables that forward raw string args via `ensureInputIsObject`).
- Documented the coercion contract with JSDoc on `BaseExecutor.getHandlerInput`, `LlmExecutor.execute`, and `ensureInputIsObject`.
- Updated the two `llm.test.ts` tests that previously locked in silent null coercion; added new base/llm tests for the null-throws + undefined-allowed + primitive-coerce contract.

## Files Changed
- `src/executor/_base.ts` — null guard + JSDoc on `getHandlerInput`
- `src/executor/llm.ts` — null guard before `?? {}`, JSDoc on `execute`
- `src/utils/modules/ensureInputIsObject.ts` — JSDoc documenting coercion semantics
- `src/executor/_base.test.ts` — new tests: null throws, primitives still coerce
- `src/executor/llm.test.ts` — replace null/undefined silent-coercion tests with null-throws + undefined-works

## Next Steps
- Pre-existing lint errors in unrelated files (`src/utils/modules/json.ts`, `src/utils/modules/pick.ts`, handlebars helpers) — not in scope here, but worth a cleanup pass in a future run.
- The `ensureInputIsObject` utility still silently coerces `null`/`undefined` at the utility level (only the executor entry points guard now). If a future run wants to tighten further, consider throwing in the utility itself after auditing all call sites — but that's a wider change that would touch the callable plugin path and likely deserves its own issue.
- The docs site (`docs/`) mentions executor input semantics and should be updated to reflect the new null-is-rejected contract — out of scope for this coder run (docs label).
